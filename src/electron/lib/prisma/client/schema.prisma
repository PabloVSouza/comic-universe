generator client {
  provider = "prisma-client-js"
  output   = "../src/electron/lib/prisma/client"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id   Int    @id @default(autoincrement())
  name String
}
