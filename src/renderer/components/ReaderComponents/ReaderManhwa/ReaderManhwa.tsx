import ReaderPageScroller from '../ReaderPageScroller/ReaderPageScroller'

const ReaderManhwa = ({ comicId }: { comicId: number }): JSX.Element => (
  <ReaderPageScroller comicId={comicId} />
)

export default ReaderManhwa
